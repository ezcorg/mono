This folder exists only for the development convenience of placing all compiled WASM component plugins in a single directory to be loaded by `witmproxy`.

This should likely exist within the `witmproxy` folder itself.