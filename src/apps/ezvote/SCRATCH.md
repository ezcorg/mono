We start with `Collective`s. `Collective`s are groups of `Entity`s (and potentially other `Collective`s), which store their state within a public blockchain/distributed ledger. `Entity`s are identified (and routable) by their public keys, may be associated with arbitrary metadata, and may use their private keys to sign and perform `Action`s (which they commit to `Collective`s ledger). `Action`s