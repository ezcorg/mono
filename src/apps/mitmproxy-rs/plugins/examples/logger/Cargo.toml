[package]
name = "logger-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
mitm_plugin_sdk = { path = "../../sdk" }
paste = "1.0"
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
