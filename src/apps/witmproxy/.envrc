# Auto-load the witmproxy dev shell for anyone using direnv + nix-direnv.
# Run `direnv allow` once. rust-analyzer (with the direnv editor integration)
# then sees the pinned nightly toolchain and native deps without a manual
# `nix develop`. Harmless if you don't use direnv.
use flake
