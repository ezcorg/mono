{
    "name": "contact-form-worker",
    "version": "2.0.0",
    "description": "Cloudflare Worker for handling contact form submissions with Mailgun and rate limiting",
    "main": "src/index.ts",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "tail": "wrangler tail",
        "typecheck": "tsc --noEmit",
        "test": "vitest",
        "test:ui": "vitest --ui",
        "test:run": "vitest run"
    },
    "dependencies": {
        "@ezdevlol/shared": "^0.0.3",
        "zod": "^3.22.0"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.8.49",
        "@cloudflare/workers-types": "^4.20250703.0",
        "@vitest/ui": "^1.0.0",
        "typescript": "^5.0.0",
        "vitest": "^3.2.4",
        "wrangler": "^4.22.0"
    }
}